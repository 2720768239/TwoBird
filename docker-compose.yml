version: '3.8'

services:
  app:
    build: .
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
